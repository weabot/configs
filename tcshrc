#VARIABLES
#set prompt="%{\033[36m%}%n%{\033[31m%}@%{\033[33m%}%m: %{\033[32m%}%B%~%b%# "
set prompt="%m:%n{%h} (%~)%# "
set autolist=ambiguous
set addsuffix
set autorehash
set autoexpand
set highlight
set implicitcd
set visiblebell
set color
set colorcat
set printexitvalue
set correct=cmd
set complete=enhance
set EDITOR=nvi
set VISUAL=nvi
set path = ( /usr/5bin $path /usr/pkg/bin /usr/scripts )

#ENVIRONMENT
setenv CC clang
setenv CXX clang++

#ALIAS
alias weather curl -4 wttr.in/quebec
alias we weather
alias cdd cd ..
alias ls ls-F
alias ttympv mpv -vo drm --profile=pseudo-gui
alias cc clang
alias c++ clang++
alias cxx clang++
alias emacs emacs -nw

#KEYBINDS
bindkey -v
